@model RelianceMkt.Models.authentication

<!DOCTYPE html>
<html lang="en" class="h-100">

<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <title>Indusind - Creative Campaign Dashboard</title>
    <!-- Favicon icon -->
    <link rel="icon" type="image/png" sizes="16x16" href="~/Content/images/favicon.png">
    <link href="~/Content/css/style.css" rel="stylesheet">
</head>

<body class="h-100">
    @*<body class="h-90" style="
        background: url('/Content/images/img.jfif') no-repeat center center fixed;
        background-size: cover;">*@

    @using (Html.BeginForm("Index", "Marketing", FormMethod.Post))
    {
        <div class="d-flex align-items-center justify-content-center vh-100">
            <div class="col-md-6 col-lg-5">
                @*<div class="card shadow-lg rounded-lg" style="border: 1px solid #f7e600; box-shadow: 0 0 0 1px #007bff;">*@
                <div class="card shadow-lg rounded-lg" style="border: 2px solid yellow; box-shadow: 0 0 0 1px #007bff;">

                    <div class="card-body p-4">
                        <div class="text-center mb-4">
                            <img class="img-fluid" src="~/Content/images/Logo16.png" alt="Logo" style="width: 200px; height: 90px;">
                        </div>

                        @if (TempData["warning"] != null)
                        {
                            <div class="alert alert-warning text-center" role="alert">
                                @TempData["warning"]
                            </div>
                        }

                        <div class="form-group">
                            <label class="mb-1"><strong>Email</strong></label>

                            @Html.TextBox(
                                "au_username",
                                TempData["au_username"] ?? "",
                                new
                                    {
                                    @class = "form-control",
                                    @placeholder = "Enter your Email",
                                    @required = "required"
                                }
                            )
                        </div>
                        <div class="form-group">
                            <label class="mb-1"><strong>Password</strong></label>
                            @Html.Password(
                                "au_password",
                                null,
                                new
                                    {
                                    @class = "form-control",
                                    @placeholder = "Enter your password",
                                    @required = "required"
                                }
                            )
                        </div>

                        @*@if (TempData["warning"] != null)
        {
            <div class="alert alert-warning text-center" role="alert">
                @TempData["warning"]
            </div>
        }

        <div class="form-group">
            <label class="mb-1"><strong>Email</strong></label>
            @Html.TextBoxFor(x => x.au_username, "", new { @class = "form-control", @placeholder = "Enter your Email", @required = "true" })
        </div>

        <div class="form-group">
            <label class="mb-1"><strong>Password</strong></label>
            @Html.PasswordFor(x => x.au_password, new { @class = "form-control", @placeholder = "Enter your password", @required = "true" })
        </div>*@

                        <div class="text-center mt-3">
                            <button type="submit" class="btn btn-primary btn-block py-2">Sign In</button>
                        </div>

                        @*<div class="form-group text-center mt-3">
        <a href="/Marketing/ForgotPassOtp" class="text-secondary">Forgot Password?</a>
        </div>*@


                        <div class="form-group text-center mt-3">
                            <a href="@Url.Action("ForgotPassOtp", "Marketing")" class="text-secondary">
                                Forgot Password?
                            </a>
                        </div>

                        @*@{
            var userRole = Session["Role"] != null ? Session["Role"].ToString() : "";
        }

        @if (userRole != "Sub-Admin(Read-Only)" && userRole != "Sub-Admin(Campaign Creator)")
        {
            <div class="form-group text-center mt-3">
                <a href="/Marketing/ForgotPassOtp" class="text-secondary">Forgot Password?</a>
            </div>
        }*@

                    </div>
                </div>
            </div>
        </div>
    }

    <!-- Scripts -->
    <script src="~/Content/vendor/global/global.min.js"></script>
    <script src="~/Content/vendor/bootstrap-select/dist/js/bootstrap-select.min.js"></script>
    <script src="~/Content/js/custom.min.js"></script>
    <script src="~/Content/js/deznav-init.js"></script>

</body>

</html>
